@model IEnumerable<GearStore.Domain.Entities.Product>

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>📦 Quản lý sản phẩm</h2>
        <a asp-action="Create" class="btn btn-success"><i class="fa-solid fa-plus"></i> Thêm mới</a>
    </div>

    <table class="table table-bordered table-hover shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Ảnh</th>
                <th>Tên sản phẩm</th>
                <th>Danh mục</th>
                <th>Giá tiền</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="width: 100px;">
                        <img src="@item.ThumbnailImage" class="img-thumbnail" style="width: 80px; height: 80px; object-fit: cover;" />
                    </td>
                    <td>@item.Name</td>
                    <td><span class="badge bg-info">@item.Category?.Name</span></td>
                    <td class="fw-bold text-danger">@item.Price.ToString("N0") đ</td>
                    <td>
                        <button class="btn btn-sm btn-primary">Sửa</button>
                        <button class="btn btn-sm btn-danger">Xóa</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>